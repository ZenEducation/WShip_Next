import React from 'react'
import ThemeConfigurator from 'components/AfterAuth/template/ThemeConfigurator'

const SidePanelContent = (props) => {
    return <ThemeConfigurator {...props} />
}

export default SidePanelContent
